Travou de novo no lock do git (HEAD.lock) — não consegui commitar por aqui. Roda isso no PowerShell:

cd "C:\XDADOS\ESPAÑOL"
Remove-Item ".git\index.lock" -ErrorAction SilentlyContinue
Remove-Item ".git\HEAD.lock" -ErrorAction SilentlyContinue
git add -A
git commit -m "Nombre del archivo del certificado usa solo el primer nombre del alumno"
git push

O que mudou (v2026.07.27.7): o PDF do certificado de nível agora baixa só com o primeiro nome do aluno no arquivo, ex: "Certificado_Nivel_Básico_Español_YA - Anderson - 07-2026.pdf". No corpo do certificado o nome completo continua aparecendo normalmente, só o nome do arquivo mudou.

Depois que subir, pode apagar este arquivo.
